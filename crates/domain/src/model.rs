//! Core domain objects (placeholder).
//!
//! `Model` (`id/key/path/artifact_kind`), `Runtime`, `Instance` (state
//! machine), `Operation` and `LoadConfig` land here in the
//! "Domain types + error catalog" job. Do not put I/O, HTTP, or storage
//! code in this module — it must stay a pure domain library.
