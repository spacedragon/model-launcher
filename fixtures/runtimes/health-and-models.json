{
  "schema_version": 1,
  "fixture": "runtime-health-and-models",
  "provenance": {
    "kind": "synthetic-pinned",
    "captured_live": false,
    "note": "The development host has no llama.cpp or NInfer installation, so these HTTP bodies were never captured live. They are synthesized from the documented OpenAI-compatible surface at the engine versions pinned in manifest.json; treat them as replaceable pinned fixtures, not field captures."
  },
  "engine_versions": {
    "llamacpp": "b5555",
    "ninfer": "0.9.2"
  },
  "cases": [
    {
      "runtime": "llamacpp",
      "kind": "health",
      "endpoint": "GET /health",
      "provenance": "synthetic-pinned",
      "body": "{\"status\":\"ok\"}\n"
    },
    {
      "runtime": "llamacpp",
      "kind": "models",
      "endpoint": "GET /v1/models",
      "provenance": "synthetic-pinned",
      "body": "{\"data\":[{\"id\":\"qwen2.5-7b-instruct-q4_k_m\",\"object\":\"model\",\"owned_by\":\"llama.cpp\",\"root\":\"/mnt/d/models/qwen2.5-7b-instruct-q4_k_m.gguf\"}],\"object\":\"list\"}"
    },
    {
      "runtime": "ninfer",
      "kind": "health",
      "endpoint": "GET /health",
      "provenance": "synthetic-pinned",
      "body": "{\"status\":\"ok\"}\n"
    },
    {
      "runtime": "ninfer",
      "kind": "models",
      "endpoint": "GET /v1/models",
      "provenance": "synthetic-pinned",
      "body": "{\"data\":[{\"id\":\"qwen-2.5-7b-instruct\",\"object\":\"model\",\"owned_by\":\"ninfer\"}],\"object\":\"list\"}"
    }
  ]
}
